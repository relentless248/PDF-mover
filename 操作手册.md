# PDF自动归类工具 —— 详细操作手册

---

## 一、程序概述

**PDF自动归类工具**是一个基于 Python + Tkinter 的桌面应用程序，用于将检测项目中的 PDF 报告文件（如 TDS 文件）自动识别、匹配并归档到对应项目文件夹的目标子目录中。程序通过 **Tesseract-OCR** 对 PDF 进行文字识别，提取项目编号（如 `ABC2025010101CN`），然后根据编号自动匹配到对应的项目文件夹，实现 PDF 报告的批量归类。

### 项目编号格式

程序默认的项目编号规则为**通用占位规则**（不含任何组织特征）：

```
[A-Z]{2,5}\d{6,10}
```

即：2~5位大写字母 + 6~10位数字。

> **重要**：实际使用请务必在「⚙ 设置」→「项目编号格式」中将规则改为贵组织实际的编号格式。

---

## 二、项目目录结构

```
PDFtool/
├── pdf_mover.py              # 主程序源代码
├── 操作手册.md               # 详细操作手册（本文件）
├── core/                     # 核心模块
│   ├── settings.py           # 集中式配置：默认值、正则派生、配置读写
│   ├── matching.py           # 纯逻辑工具：年份提取/文件夹匹配/路径防碰撞
│   ├── settings_dialog.py    # 可视化配置对话框（设置面板）
│   └── __init__.py
├── tests/                    # 单元测试（不依赖界面，可独立运行）
│   └── test_core_logic.py
├── config.json               # 配置文件（存储目标根目录列表）
├── settings.json             # 用户实际配置（首次保存设置时自动生成，不提交）
├── settings.example.json     # 通用配置模板（提交版本库）
├── YearArchiver/             # 未找到项目按年份归档工具
│   ├── year_archiver.py
│   └── run_archiver.bat
├── run.bat                   # 启动脚本
├── install_deps.bat          # 一键安装 Python 依赖脚本
├── requirements.txt          # Python 依赖清单
├── app.ico                   # 程序图标
├── index_cache.json          # 索引缓存元数据
├── index_cache_chunk_*.json  # 索引缓存分块文件
├── run.log                   # 运行日志（滚动覆盖，排障时查看）
├── Records/                  # 操作记录输出目录
├── Temp/                     # 临时文件目录（每次启动自动清理）
├── Tesseract-OCR/            # OCR引擎（Tesseract 5.x）
│   ├── tesseract.exe         # 可执行文件
│   └── tessdata/             # 语言数据包
│       ├── chi_sim.traineddata  # 简体中文
│       ├── eng.traineddata      # 英文
│       └── pdf.ttf              # 字体
├── poppler-25.12.0/          # PDF渲染库（用于PDF→图片转换）
└── python/                   # 嵌入式 Python 3.13 运行时
```

> **说明**：`config.json` 存储你的目标根目录列表；`settings.json` 存储通过「设置」面板修改的配置，两者均为个人配置，不纳入版本控制（见 .gitignore）。

---

## 三、启动方式

### 通过 `run.bat` 启动

双击 `run.bat` 即可启动程序。脚本内容：

```bat
@echo off
chcp 65001 >nul
"%~dp0python\python.exe" "%~dp0pdf_mover.py"
pause
```

- `chcp 65001`：设置控制台编码为UTF-8，保证中文正常显示
- 使用项目自带的嵌入式 Python 运行，无需系统安装 Python

### 启动时自检

程序启动时会检查以下依赖是否存在，如果缺失会弹出错误对话框：

| 检查项 | 路径 | 说明 |
|--------|------|------|
| Tesseract | `Tesseract-OCR\tesseract.exe` | OCR引擎 |
| Tessdata | `Tesseract-OCR\tessdata\` | 语言数据包目录 |
| Poppler | `poppler-25.12.0\Library\bin\` | PDF渲染引擎 |

---

## 四、界面布局详解

程序主窗口尺寸为 **1000×850**，最小尺寸 **800×700**，分为以下区域：

### 4.1 顶部：文件源设置区

| 控件 | 说明 |
|------|------|
| **源文件夹** 输入框 | 选择/显示当前待处理的 PDF 源文件夹路径 |
| **浏览** 按钮 | 打开文件夹选择对话框，选择 PDF 文件所在的源文件夹 |
| **刷新列表** 按钮 | 重新扫描源文件夹，更新 PDF 文件列表 |
| **⚙ 设置** 按钮 | 打开「项目设置」面板，配置项目编号格式、识别参数、归档子目录（详见第十一章） |
| **❓ 帮助** 按钮 | 打开程序内置使用说明窗口（随程序分发，内容与本手册互补） |

### 4.2 年份选择栏

| 控件 | 说明 |
|------|------|
| **启用年份筛选** 复选框 | 默认勾选。**不需要年份功能的用户取消勾选即可**：年份下拉自动禁用并重置为"全部"，扫描与统计将包含全部年份；勾选状态即时保存到 settings.json，下次启动保持 |
| **分拣年份** 下拉框 | 选择要处理的年份（如"25"代表2025年）。默认为"全部"。切换后需点击"重建索引"生效。仅在勾选「启用年份筛选」时可用 |
| **提示文字** | "输入年份如25 → 点击重建索引" |

> **内部逻辑**：年份过滤是在构建索引时生效的。选中年份后，只会将该年份的项目文件夹加入索引，其他年份的文件夹将被过滤掉。年份从项目编号的第5位开始提取2位（如 `ABCD2025010101CN` 中提取 `25`）。取消勾选「启用年份筛选」后，索引构建不再做任何年份过滤。

### 4.3 目标范围区

| 控件 | 说明 |
|------|------|
| **根目录列表** | 显示已添加的所有目标根目录（项目文件夹所在的父目录） |
| **添加根目录** | 添加一个目标根目录，同时触发索引重建 |
| **删除选中** | 删除选中的根目录，清理缓存并重建索引 |
| **重建索引** | 删除所有缓存，重新扫描所有根目录下的项目文件夹 |
| **刷新选中** | 仅刷新选中的根目录（增量更新，不清除其他根目录的索引） |

### 4.4 PDF文件列表区（左侧）

- 显示源文件夹中所有 `.pdf` 文件
- 点击列表项触发 PDF 选取 → OCR 识别 → 项目编号提取 → 文件夹匹配
- 自动运行模式下会按顺序自动处理

### 4.5 PDF预览区（右侧）

| 控件 | 说明 |
|------|------|
| **放大/缩小** 按钮 | 调整预览图缩放比例（0.5x ~ 3.0x） |
| **全尺寸预览** 按钮 | 在新窗口中打开150 DPI的全尺寸预览（可滚动） |
| **查看识别文本** 按钮 | 在新窗口中显示OCR识别出的所有文字内容，可一键复制全部文本 |
| **预览画布** | 鼠标拖拽平移预览图；`Ctrl+滚轮` 缩放；鼠标悬停变为手型光标 |

### 4.6 操作区（底部）

#### 匹配信息

| 控件 | 说明 |
|------|------|
| **目标文件夹** 下拉框 | 显示匹配到的项目文件夹路径，可手动编辑或点击浏览选择 |
| **浏览** 按钮 | 手动选择目标文件夹（必须是已添加根目录下的路径） |
| **手动修正编号** 输入框 | 手动输入或修改项目编号 |
| **重新匹配** 按钮 | 使用手动修正的编号重新搜索匹配文件夹 |

#### 模式选项（Checkbutton）

| 选项 | 说明 |
|------|------|
| **多项目复制模式** | 勾选后，文件操作变为**复制**而非移动，原文件保留在源文件夹 |
| **🚀 全自动不干预模式** | 勾选后，自动运行时遇到异常/无法匹配等情况自动跳过，不弹窗询问 |
| **😴 不识别模式** | 勾选后跳过OCR文字识别，仅显示PDF预览图片，适用于只查看不需要识别的情况 |

#### 操作按钮

| 按钮 | 颜色 | 功能 |
|------|------|------|
| **确认移动** | 蓝色 | 将当前PDF移动/复制到目标文件夹的目标子目录中 |
| **跳过** | 默认 | 跳过当前文件，自动选中下一个；根据识别结果可能自动重命名 |
| **未找到项目** | 橙色 | 将文件重命名为"未找到项目\_编号.pdf"并记录到未找到Excel |
| **旋转90°** | 绿色 | 将PDF旋转90°后重新OCR识别（处理横向/倒置扫描件） |
| **自动运行** | 绿色 | 启动自动运行模式，按列表顺序逐个批量处理所有PDF |
| **继续自动** | 黄色 | 当自动运行暂停后，恢复自动运行 |
| **退出** | 默认 | 退出程序，自动保存所有记录文件 |

#### 状态栏

- 显示当前操作状态、文件处理进度等信息

#### 索引进度

- 右下角显示文件夹索引构建进度，格式如"索引进度：500/1200"

---

## 五、核心内部逻辑详解

### 5.1 文件夹索引构建 (`build_folder_index_thread`)

**触发时机**：
- 添加/删除根目录
- 点击"重建索引"
- 点击"刷新选中"
- 程序启动时索引缓存过期

**流程**：

1. **第一阶段（统计）**：遍历所有根目录，使用 `os.scandir` 扫描，跳过系统目录（`$RECYCLE.BIN`、`System Volume Information`、`Temp`、`tmp`等），统计包含项目编号的子文件夹总数

2. **第二阶段（收集）**：再次遍历，将匹配项目编号正则的文件夹路径收集到列表中。每100个文件夹更新一次UI进度

3. **完成后**：将结果写入分块缓存文件（超过10000条时自动分块存储），显示统计摘要弹窗

**年份过滤**：在扫描时提取文件夹名中的项目编号，从编号中提取2位年份（如 `ABCD2025010101CN` → `25`），与用户选择的年份比对，不匹配则跳过。

**超时卡住检测**：如果索引进度超过30秒没有更新，会弹窗提示用户索引可能卡住（通常是遇到了无法访问的网络路径）。

### 5.2 PDF OCR识别流程 (`process_pdf_thread` → `on_ocr_done`)

**触发时机**：用户在文件列表中点击PDF文件

**流程**：

1. **缓存检查**：生成缓存键 `{文件路径}_{修改时间}`，如果命中OCR缓存则直接使用缓存结果，无需重新识别

2. **路径安全处理** (`get_safe_pdf_path`)：
   - 如果路径包含非ASCII字符（如中文路径）或为网络路径（`\\`开头），先将文件复制到 `Temp/` 临时目录后再处理
   - 处理完成后自动删除临时文件

3. **PDF → 图片** (`pdf2image.convert_from_path`)：
   - 使用 Poppler 渲染引擎将PDF转换为图片
   - DPI = 400，高质量渲染
   - 最多处理前5页（可在设置面板「识别参数」中修改最大识别页数）
   - 超时限制30秒，超时则抛出 `TimeoutError`
   - **识别与预览共用同一次渲染**：首页渲染结果直接用作界面预览图，不做第二次渲染

4. **OCR识别** (`pytesseract.image_to_string`)：
   - 语言包：`eng+chi_sim`（英文 + 简体中文混合识别）
   - 配置参数：`--psm 3 --oem 3 -c preserve_interword_spaces=1`
     - **PSM 3**：全自动页面分割模式（自动检测文本区域和文本方向）
     - **OEM 3**：使用LSTM神经网络引擎（识别精度最高）
     - **preserve_interword_spaces**：保留单词间的空格
   - 超时限制：每页20秒

5. **项目编号提取** (`extract_all_project_ids` / `extract_all_see_ids`)：
   - 从OCR文本中用正则找出所有匹配项目编号规则的项目编号（去重并保持原始顺序）
   - 从文本中额外提取所有 `see`/`关联` 编号（格式如 `see ABC2025010101CN` 或 `(ABC2025010101CN)`）

6. **文件名兜底**：如果OCR未能找到任何编号，尝试从文件名本身中提取关联编号

7. **写入OCR缓存**：将识别文本存入 `OrderedDict`（最大容量50条，LRU淘汰策略）

### 5.3 文件夹搜索匹配 (`search_folders` → `_do_search_folders`)

**匹配规则** (`folder_matches`)：
- 文件夹名以项目编号开头（不区分大小写）
- 或文件夹名中包含完整的项目编号（按单词边界匹配）

**搜索优化**：
- 手动操作时：延迟300毫秒后搜索（防抖处理，避免频繁输入触发搜索）
- 自动模式：立即搜索，无延迟
- 搜索缓存：最大容量100条，LRU淘汰策略
- 强制刷新：当 `force_refresh=True` 时，先清除对应项目的缓存再重新搜索

### 5.4 目标子文件夹查找 (`find_test_data_folders`)

在匹配到的项目文件夹中，查找包含目标子目录名的子文件夹。匹配规则为：**不区分大小写、忽略空格/下划线/横线等分隔符差异**的包含匹配。默认目标子目录名为 `Data`，因此：

- ✅ 匹配：`Data`、`data`、`01 Data`、`data_files`、`TestData`
- ❌ 不匹配：不包含目标名的文件夹（如 `xxx`）

目标子目录名可在设置面板「归档与子目录」页中修改。

**结果处理**：
- **0个 Test Data** → 标记为"无Test Data"，尝试see关联处理
- **1个 Test Data** → 自动作为目标文件夹
- **多个 Test Data** → 需要用户手动选择其中一个

### 5.5 自动运行流程 (`auto_run_next` → `auto_check_and_process`)

完整的自动处理逻辑按优先级依次执行：

1. **选择当前文件** → 触发OCR识别

2. **识别完成后自动进入判断流程**：

3. **未识别到任何编号** → 自动旋转90°重试一次 → 仍无编号 → 重命名为"未识别\_原文件名.pdf"

4. **识别到多个项目编号** → 进入多项目处理模式（每个项目号都复制一份到对应文件夹）

5. **匹配到1个文件夹 → 有1个Test Data** → 自动执行移动/复制操作

6. **匹配到多个文件夹** → 自动复制到所有匹配文件夹的目标子目录

7. **匹配到0个文件夹** → 最多重试2次（强制刷新搜索缓存后重试）→ 仍无匹配 → 重命名为"未找到项目\_编号.pdf"

8. **检测到文件名格式为"未找到项目\_xxx.pdf"** → 自动再次尝试归档（称为"第二轮处理"）

9. **无Test Data → 有关联项目号(see)** → 尝试归档到关联项目的Test Data

**暂停/超时机制**：
- 遇到需要人工确认的情况时，弹出20秒倒计时对话框（`AutoPauseDialog`）
- 对话框有三个操作：立即处理、跳过、关闭（等于跳过）
- 「跳过」「关闭」或倒计时结束，都会执行默认操作（跳过/重命名），且整个对话框生命周期内只会触发一次
- 「立即处理」：关闭对话框并取消倒计时，**不会**触发自动跳过，随后可在主界面手动处理该文件

### 5.6 See关联处理 (`auto_handle_see_relation`)

当PDF中识别到 `see ABC2025010101CN` 这样的关联标记时：
- 在当前项目找不到Test Data的情况下，尝试查找关联项目的Test Data
- 如果关联项目有且仅有1个目标子目录，自动归档到关联项目的目标子目录
- 操作记录中文件名记为"原始编号(关联xxx)"

### 5.7 多项目处理 (`auto_handle_multi_projects`)

当OCR识别到多个项目编号时（同一PDF涉及多个项目）：
- 对每个编号分别尝试归档（强制使用复制模式，保留原文件）
- 每个编号的处理结果（成功/失败/具体原因）写入**多项目对账Excel**
- 全部编号处理完后，原文件重命名为"多项目\_原文件名.pdf"留在源文件夹

---

## 六、Excel记录文件详解

程序根据源文件夹名自动生成三份Excel文件，存放在 `Records/` 目录中：

### 6.1 已找到\_{源文件夹名}.xlsx

记录所有成功归档的文件：

| 列名 | 说明 |
|------|------|
| 操作时间 | 归档操作的时间戳，格式 `YYYY-MM-DD HH:MM:SS` |
| 源文件 | 原始PDF文件名 |
| 项目编号 | 识别/匹配到的项目编号 |
| 目标文件夹 | 归档到的Test Data文件夹完整路径 |
| 操作类型 | 移动 / 复制 / 复制(关联) / 复制(多项目) / 自动归档未找到项目 |

### 6.2 未找到\_{源文件夹名}.xlsx

记录未能成功归档的文件：

| 列名 | 说明 |
|------|------|
| 操作时间 | 操作时间戳 |
| 原文件名 | 原始PDF文件名 |
| 重命名后文件名 | 重命名后的新文件名 |
| 项目编号 | 识别到的项目编号（可能为空） |

### 6.3 多项目对账\_{源文件夹名}.xlsx

当处理包含多个项目编号的PDF时生成的详细对账记录：

| 列名 | 说明 |
|------|------|
| 操作时间 | 操作时间戳 |
| 源文件名 | 原始PDF文件名 |
| 本次识别到的所有项目号 | 用逗号分隔的所有识别到的项目编号 |
| 当前处理的项目号 | 每行对应处理的一个项目编号 |
| 处理状态 | 成功 / 失败 |
| 备注/失败原因 | 匹配数量异常、Test Data异常、复制异常等具体原因 |

### 6.4 记录写入机制

- 使用内存缓存 `excel_caches` 持有打开的工作簿对象，避免频繁打开/关闭文件
- 每累积 **10条记录** 自动保存一次
- 使用**原子写入**机制：数据先写入 `.tmp` 临时文件，成功后再 `os.replace()` 覆盖原文件，防止写入中断导致文件损坏
- 退出程序时自动保存所有缓存中的记录，并提示是否打开 Records 文件夹查看
- 如果Excel文件被其他程序（如Excel）打开占用，会提示是否生成带时间戳的备份文件

---

## 七、OCR缓存与搜索缓存

| 缓存类型 | 容量 | 淘汰策略 | 缓存键 |
|----------|------|----------|--------|
| OCR缓存 (`ocr_cache`) | 50条 | LRU（最近最少使用） | `{文件路径}_{文件修改时间}` |
| 搜索缓存 (`search_cache`) | 100条 | LRU（最近最少使用） | 项目编号 |

**缓存失效条件**：
- 文件修改时间发生变化 → OCR缓存自动失效（因为缓存键包含修改时间）
- 索引重建（添加/删除根目录、点击重建索引） → 搜索缓存和OCR缓存均清空
- 强制刷新搜索 → 仅清除对应项目编号的搜索缓存

---

## 八、文件操作安全机制

### 8.1 原子写入

所有 JSON 配置文件和 Excel 文件均使用原子写入机制：
1. 数据先写入 `.tmp` 临时文件
2. 调用 `os.replace()` 将临时文件原子性地替换原文件
3. 如果写入失败，自动删除临时文件

### 8.2 文件操作重试

文件移动/复制/重命名时遇到 `PermissionError`（文件正被其他进程占用），最多重试 **5次**，每次间隔2秒。其他类型错误（文件不存在、路径无效、权限拒绝等）直接抛出，不重试。

### 8.3 目标路径安全校验

所有目标文件夹必须在用户已添加的根目录范围内（通过 `is_target_path_valid` 校验），防止误操作将文件移动到不允许的目录。

### 8.4 同名文件处理

如果目标路径已存在同名文件（`TDS.pdf`），自动添加序号后缀：
- `TDS_1.pdf`、`TDS_2.pdf`、`TDS_3.pdf` ... 以此类推

### 8.5 Windows长路径兼容

通过 `get_windows_compatible_path` 将文件路径转换为 `\\?\` 前缀格式，突破 Windows 系统默认的 260 字符路径长度限制。网络路径（UNC）也会转换为 `\\?\UNC\` 格式。

---

## 九、配置管理

### config.json 结构

存储用户添加的目标根目录（**下例路径为虚构示例**）：

```json
{
    "roots": [
        {"path": "D:/项目档案/2026年", "depth": 1},
        {"path": "D:/项目档案/2025年", "depth": 2}
    ]
}
```

- `path`：根目录完整路径；`depth`：该根目录的扫描深度（1~5层），可在「编辑深度」中调整
- 旧版纯路径列表格式（`"target_roots": ["D:/..."]`）仍可被程序读取兼容，程序保存时统一写为新格式

### 运行日志

程序运行日志写入根目录 `run.log`（同时输出到控制台），超过 2MB 自动滚动备份（最多保留 2 份）。遇到程序行为异常时，可优先查看该文件定位问题；日志文件属运行产物，不纳入版本控制。

### 索引缓存

- 缓存有效期：**7天（604800秒）**
- 当根目录列表发生变化时，缓存自动失效
- 超过10000条时自动分块存储：`index_cache_chunk_0.json`, `index_cache_chunk_1.json` ...
- 可通过点击"重建索引"按钮强制刷新

---

## 十、典型操作流程

### 场景一：手动逐份处理（适合少量文件）

1. 首次使用 → 点击"添加根目录"选择项目文件夹所在的父目录 → 等待索引构建完成
2. 点击"浏览"选择PDF源文件夹 → 程序自动扫描并列出所有PDF文件
3. 在左侧文件列表中点击第一个PDF → 等待OCR识别完成，右侧出现预览图
4. 检查预览图中识别到的项目编号是否正确
5. 如果图片方向不对（横版/倒置），点击"旋转90°"重新识别
6. 确认目标文件夹正确 → 点击"确认移动"完成归档
7. 重复步骤3-6处理其余文件

### 场景二：自动批量处理（适合大量文件）

1. 确保索引已构建完毕
2. 选择源文件夹，点击"刷新列表"
3. 根据需要勾选模式：
   - "多项目复制模式"：归档后保留原文件在源文件夹
   - "🚀 全自动不干预模式"：遇到异常不弹窗，自动跳过
4. 点击"自动运行"
5. 程序自动按顺序处理每个文件，遇到需要确认时弹出20秒倒计时对话框
6. 处理完毕后弹出统计摘要，显示各类剩余文件数量

### 场景三：第二轮处理（处理"未找到项目"文件）

1. 将之前标记为"未找到项目\_xxx.pdf"的文件放回源文件夹
2. 刷新列表
3. 启动自动运行
4. 程序会自动检测文件名中的项目编号，再次尝试匹配和归档

---

## 十一、设置面板（可视化配置）

程序的所有关键参数（项目编号规则、识别参数、归档设置）均可在图形界面中修改，**无需编辑代码**。

### 11.1 打开方式

点击主界面右上角的 **「⚙ 设置」** 按钮，打开「项目设置」面板。面板包含四个页签：**项目编号格式**、**识别参数**、**归档与子目录**、**年份管理**。

### 11.2 项目编号格式（页签一）

默认项目编号规则为**通用占位规则**（不含任何组织特征）：

```
[A-Z]{2,5}\d{6,10}
```

即：2~5位大写字母 + 6~10位数字。程序安装后默认不含组织特征；首次使用请在此页将规则改为贵组织实际的编号格式（例如 `(?:AB|CD)\d{10}[A-Z]{2}`，下例为虚构格式，仅示意「固定前缀 + 数字段 + 固定后缀」的组合方式）。

**段式建模（默认）**：将编号拆成若干「段」逐个建模，从上到下依次拼接生成正则。

| 段类型 | 说明 |
|--------|------|
| 固定文本 | 固定出现的字符，如 `SH`、`SU`；多个值用逗号分隔（如 `SH,SU` 生成 `(?:SH\|SU)`） |
| 数字 | 指定位数范围，如 10 位 → `\d{10}` |
| 字母(大写/小写/任意) | 指定位数范围，如 2 位大写 → `[A-Z]{2}` |
| 自定义字符集 | 手动填写字符集（如 `A-Z0-9`）并指定位数范围 |

支持的操作：**添加段**、**编辑**、**删除**、**上移/下移**调整顺序；每个段可勾选「该段可省略（可选）」。

- **正则预览**：下方实时显示由分段自动生成的正则表达式；
- **测试功能**：在测试输入框粘贴一个真实编号，实时验证是否匹配（✔ 匹配 / ✘ 不匹配）。

**高级模式**：勾选「高级模式（直接编辑正则）」可手写正则表达式，适合有正则基础的进阶用户。取消勾选后可回到段式模式（本工具能自动解析的正则会回显为分段，无法解析的复杂正则需手动重建）。

### 11.3 识别参数（页签二）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 识别语言 | 中文 + 英文（复选框） | 简体中文 / 英文可单独勾选或同选，自动生成语言串（如 `eng+chi_sim`），实时预览；至少选一种 |
| 识别分辨率 DPI | 400 | 渲染分辨率，越高越清晰但越慢 |
| 最大识别页数 | 5 | 每个 PDF 最多识别的页数 |
| OCR 引擎配置(高级) | `--psm 3 --oem 3 -c preserve_interword_spaces=1` | Tesseract 引擎参数，一般无需修改 |
| 每页超时(秒) | 20 | 单页 OCR 超时 |
| PDF转换超时(秒) | 30 | PDF→图片转换超时 |
| 预览超时(秒) | 15 | 预览图生成超时 |
| OCR缓存条数 | 50 | OCR 结果缓存容量 |
| 搜索缓存条数 | 100 | 文件夹搜索缓存容量 |
| 自动旋转重试 | 开启 | 未识别到编号时自动旋转重试一次 |
| 旋转步长(度) | 90 | 自动旋转的角度步长 |

### 11.4 归档与子目录（页签三）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 目标子文件夹名 | `Data` | 归档目标子目录名。匹配规则：**不区分大小写、忽略空格/下划线/横线等分隔符差异**（如填 `Data` 可匹配 `data`、`01 Data`、`data_files`） |
| 归档文件名(不含扩展名) | `TDS` | 复制/移动后的文件名，如 `TDS.pdf` |
| 文件夹匹配规则1 | 勾选 | 文件夹名以编号开头即匹配 |
| 文件夹匹配规则2 | 勾选 | 文件夹名包含完整编号（按单词边界）即匹配 |

### 11.5 年份管理（页签四）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 编号前缀长度 | 4 | 年份提取时跳过的前缀位数（即编号开头的字母段位数，如 `ABCD202501` 中跳过 `ABCD` 后取 `25` 作为年份） |
| 世纪前缀 | `20` | 年份提取的世纪前缀（如编号内 `2025` 提取为年份 `25`） |
| 未识别年份归档目录名 | `未分类` | YearArchiver 归档无法识别年份的文件时使用的目录名 |
| 可用年份分类 | 空 | 预置到主界面「分拣年份」下拉（如填 24、25、26）；留空则只显示扫描发现的年份 |

> 年份提取规则与 YearArchiver 共用同一套配置，修改后两个工具同时生效。

### 11.6 保存 / 取消 / 恢复默认

- **保存**：将当前设置写入 `settings.json` 并**立即生效**（无需重启程序）；
- **取消**：关闭面板，不保存任何修改；
- **恢复默认**：将所有设置恢复为出厂默认值（即本手册各表格中的默认值）并保存。

> **说明**：`settings.json` 在首次点击「保存」时自动生成于程序根目录，存放你的个性化配置。该文件不纳入版本控制（已在 .gitignore 中排除），请勿手动删除；如需还原初始配置，在设置面板点击「恢复默认」即可。`settings.example.json` 是随程序分发的配置模板，仅作参考，程序不会读取它。

---

## 十二、年份归档工具（YearArchiver）

一个独立的小工具，用于把散落的「未找到项目_xxx.pdf」文件按年份批量归档，方便按年度整理历史文件。

### 12.1 启动方式

双击 `YearArchiver\run_archiver.bat`，或在命令行运行：

```bat
python\python.exe YearArchiver\year_archiver.py
```

### 12.2 使用流程

1. **导入路径（源文件夹）**：点击「浏览」选择包含"未找到项目"PDF 的文件夹；
2. **导出路径（目标文件夹）**：点击「浏览」选择归档目标文件夹（将在此文件夹下自动创建年份子文件夹）；
3. 点击 **「扫描文件」**，程序按主程序的编号规则从文件名中识别项目号，并推导归档年份，预览列表显示每个文件的操作类型；
4. 确认无误后点击 **「开始归档」**：
   - 自动创建年份子文件夹（如 `2025`、`2026`...）；
   - 无法识别年份的文件归档到「未分类」文件夹；
   - 同一个 PDF 含多个项目号时，会复制到每个项目号对应的年份文件夹；
   - 处理完成后自动生成 `归档报告_时间戳.txt` 保存在目标文件夹中。

> **说明**：年份提取规则与主程序一致（默认跳过编号前 4 位前缀，取 2 位年份），可在主程序设置面板「归档与子目录」页中调整；本工具共用同一套配置。

---

## 十三、常见问题与注意事项

### Q: 程序提示"找不到Tesseract/Poppler"怎么办？

A: 确保 `Tesseract-OCR` 和 `poppler-25.12.0` 文件夹在程序根目录下，且保持完整的目录结构。如果是完整拷贝的U盘版本，这些依赖应该已包含在程序中。

### Q: 网络路径（如 `\\server\share`）可以使用吗？

A: 程序会自动跳过网络路径作为目标根目录。如需处理网络路径上的文件，建议先将文件复制到本地。

### Q: 为什么有些PDF识别不到项目编号？

A: 可能原因：PDF是纯图片扫描件且方向不对（尝试点击"旋转90°"）；图片质量太差（模糊/倾斜）；项目编号格式不符合正则表达式。可以勾选"不识别模式"仅预览图片，然后手动输入编号。

### Q: 自动运行暂停后如何恢复？

A: 点击底部"继续自动"按钮即可恢复自动运行。

### Q: 退出程序时提示"记录未导出"是什么意思？

A: 程序会提示是否打开 Records 文件夹查看本次操作生成的Excel记录。选择"是"打开文件夹，选择"否"直接退出，选择"取消"留在程序中。
